<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="600" d:DesignHeight="500"
             x:Class="ImmersingPicker.Controls.BatchSeatEditDialog">
    <Grid RowDefinitions="Auto,*,Auto" Margin="20">
        <!-- 说明文本 -->
        <TextBlock Grid.Row="0" 
                   Text="批量设置所有学生的座位行列（格式：行，列）"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Margin="0,0,0,15"/>
        
        <!-- 学生列表 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="StudentsStackPanel" Spacing="10"/>
        </ScrollViewer>
        
        <!-- 底部按钮 -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Spacing="10"
                    Margin="0,15,0,0">
            <Button x:Name="CancelButton" Content="取消" Width="100"/>
            <Button x:Name="VerifyButton" Content="验证并保存" Width="120" Classes="Accent" IsVisible="False"/>
            <Button x:Name="SaveButton" Content="保存" Width="100" Classes="Accent"/>
        </StackPanel>
    </Grid>
</UserControl>
